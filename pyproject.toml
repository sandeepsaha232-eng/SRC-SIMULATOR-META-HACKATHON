[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sre-fleet-gym"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = ["openenv-core>=0.2.0", "fastapi", "uvicorn"]

[project.scripts]
server = "server.app:main"
